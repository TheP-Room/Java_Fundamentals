public class ControlFlow {
    public static void main(String[] args){
        /*Comparison operators
            ==, != , <=, >=, <,>
        */
        /*Logical operators
            && (and) , || (or), !
        */
        /* If statements
            {} - use it for multiple code, for single line you can leave it (codeblock)
            if () {
                code
            }
            else if ()
                code
            else
                code
        */
        /* Ternary operator
            ? :
            String name = (condition) ? "value1" : "value2";
            if condition true then value 1, else value 2

            for multiple conditions
            String name = (condition) ? "v1" : (c2) ? "v2" : (c3) ? "v3" : "v4";
        */
        /* Switch statements
            switch(variable){
                case "value1":
                    code
                    break;
                case "value2":
                    code
                    break;
                default:
                    code
            }
        */
        /* For loop
            for(initialize, condition, increment/decrement){
                code
            }

            while loop are used when we don't know how many time will the code repeat

           While loop
            initialization;
            while(condition){
                increment/decrement
            }

            it executes atleast once
           Do While loop
           initialization;
           do{
           } while (condition);

           for each loop
           for(VariableType i : variable){}
        */
        /* Break statement terminates the loop and continue statement ignores the
            code left after it inside codeblock and re run the loop
        */
    }
}
